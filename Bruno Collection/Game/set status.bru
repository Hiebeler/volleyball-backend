meta {
  name: set status
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/api/games/4/status
  body: json
  auth: inherit
}

body:json {
  {
    "status": "PLANNED"
  }
}

settings {
  encodeUrl: true
}
